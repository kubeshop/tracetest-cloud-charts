apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tracetest-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "tracetest-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tracetest-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
